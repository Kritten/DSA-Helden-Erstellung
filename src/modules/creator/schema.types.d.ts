/**
 * JSON Schema für die DSA Helden Erstellung
 */
export interface JSONSchemaFurDieDSAHeldenErstellung {
  /**
     * Die einzelnen Schritte der Erstellung
     */
  steps: {
    title: string;
  }[];
}
